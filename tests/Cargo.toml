[package]
name = "selium-tests"
version = "0.1.0"
edition = "2021"

[dev-dependencies]
anyhow = "1.0"
futures = "0.3"
selium = { path = "../client" }
tokio = { version = "1.29", features = ["macros"] }
