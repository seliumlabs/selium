## 1. Macro: Parse `wire` parameter

- [x] 1.1 Add `wire` key parsing to `expand()` in `schema.rs` — accept `wire = TypeName` as either a bare ident or a string literal parsed as ident
- [x] 1.2 Store parsed wire name (as `Option<syn::Ident>`) alongside existing `path`, `fqname`, `binding_path`

## 2. Macro: Generic parameter detection

- [x] 2.1 Add `collect_generic_params()` helper that extracts generic param idents from `syn::Generics`
- [x] 2.2 Add `field_is_generic()` helper that checks if a field type references any generic param ident (handles direct `K`, `Option<K>`, etc.)
- [x] 2.3 Add `field_is_option_of_generic()` helper for `Option<V>` where `V` is generic
- [x] 2.4 Add `field_is_option_of_scalar()` helper for `Option<u64>` etc.

## 3. Macro: Wire struct generation

- [x] 3.1 Add `generate_wire_struct()` — builds a `syn::ItemStruct` with the wire name, `#[schema(...)]` attribute (same path/ty/binding), and transformed fields
- [x] 3.2 Implement field type transformation: generic `K` → `Vec<u8>`, `Option<generic>` → `Vec<u8>`, `Option<scalar>` → `ScalarType`, concrete types pass through
- [x] 3.3 Implement field name transformation: generic fields get `_bytes` suffix appended
- [x] 3.4 Strip wire struct doc comments or generate clear doc: "Wire type for {DomainName}, generated by #[schema]"

## 4. Macro: Bridge FlatMsg impl generation

- [x] 4.1 Add `generate_bridge_encode()` — generates `FlatMsg::encode` on domain type that converts each field, constructs wire struct, and calls `FlatMsg::encode(&wire)`
- [x] 4.2 Add `generate_bridge_decode()` — generates `FlatMsg::decode` on domain type that calls `FlatMsg::decode::<WireType>` then converts wire fields back
- [x] 4.3 Add `generate_bridge_has_schema()` — generates `HasSchema` impl delegating to `{WireName}Schema`
- [x] 4.4 Emit correct trait bounds: `K: FlatMsg` for each generic param used in byte-serialized fields

## 5. Macro: Error case for generic without wire

- [x] 5.1 When `#[schema]` has no `wire` param but the struct has generics, emit a compile error: `"struct has generic type parameters; add wire = WireTypeName to use schema"`
- [x] 5.2 Ensure non-generic structs without `wire` continue to work unchanged (existing behavior)

## 6. Macro: Wire the expanded output together

- [x] 6.1 In `expand_struct()`, branch on presence of `wire` param: if set, call the wire generation + bridge impl path; otherwise use existing direct-impl path
- [x] 6.2 Emit token stream in correct order: domain struct (cleaned) → wire struct (with `#[schema]`) → domain `FlatMsg`/`HasSchema` impls
- [x] 6.3 Verify the wire struct's `#[schema]` generates its own output (the existing `expand_struct` handles this via recursive or separate expansion)

## 7. Update LiveTableMessage to use generated wire

- [x] 7.1 Remove `LiveTableMessageWire` struct definition from `crates/core/guest/src/io/tables.rs`
- [x] 7.2 Add `#[schema(wire = LiveTableMessageWire, ...)]` to `LiveTableMessage<K, V>` in `tables.rs`
- [x] 7.3 Remove manual `FlatMsg for LiveTableMessage<K, V>` impl (encode + decode)
- [x] 7.4 Remove manual `HasSchema for LiveTableMessage<K, V>` impl
- [x] 7.5 Import generated items from macro output if needed (they should be in scope automatically since proc-macro emits them in the same module)

## 8. Verify and test

- [x] 8.1 Run `cargo build -p selium-guest` to verify compilation
- [x] 8.2 Run `cargo test -p selium-guest` to verify all existing tests pass (especially live table round-trip tests in `tables.rs`)
- [x] 8.3 Add a compile-fail test: generic struct with `#[schema]` but no `wire` param produces the expected error
- [x] 8.4 Verify `InterfaceMetadataWire` and other existing non-generic wire types compile unchanged
