[package]
name = "web-time"
version = "1.1.0"
edition = "2021"
rust-version = "1.81"
description = "Vendored minimal subset of web-time (custom time source only)"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
