namespace examples.echo;

table EchoRequest {
  /// Directory entry where the caller has registered a shared response channel.
  response_channel:uint64;
  /// Payload to echo back to the caller.
  msg:string;
}

table EchoResponse {
  /// The echoed payload.
  msg:string;
}

root_type EchoRequest;
file_identifier "ECHO";
